<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:tx="http://www.springframework.org/schema/tx" xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc.xsd">

    <context:annotation-config/>

    <context:component-scan base-package="cn.macrotea.showcase"/>

    <bean id="dataSource" class="org.springframework.jdbc.datasource.SimpleDriverDataSource">
        <property name="driverClass" value="${jdbc.driverClassName}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

    <!--ORM Config-->

    <beans profile="hibernate4">

        <bean id="sessionFactory" class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
            <property name="dataSource" ref="dataSource"/>
            <property name="packagesToScan" value="cn.macrotea.showcase.model"/>
            <property name="hibernateProperties">
                <props>
                    <prop key="hibernate.dialect">${hibernate.dialect}</prop>
                    <prop key="hibernate.show_sql">true</prop>
                    <prop key="hibernate.format_sql">true</prop>
                    <prop key="hibernate.hbm2ddl.auto">update</prop>
                    <prop key="hibernate.current_session_context_class">org.springframework.orm.hibernate4.SpringSessionContext</prop>
                </props>
            </property>
        </bean>

        <tx:annotation-driven transaction-manager="transactionManager" />

        <bean id="transactionManager" class="org.springframework.orm.hibernate4.HibernateTransactionManager">
            <property name="sessionFactory" ref="sessionFactory"/>
        </bean>
    </beans>

    <beans profile="jdbcTemplate">

        <bean id="articleDao" class="cn.macrotea.showcase.dao.ArticleDao">
            <property name="dataSource" ref="dataSource"/>
        </bean>

        <bean id="articleTagDao" class="cn.macrotea.showcase.dao.ArticleTagDao">
            <property name="dataSource" ref="dataSource"/>
        </bean>

    </beans>

    <!--JDBC Config File-->

    <beans profile="h2">
        <context:property-placeholder location="classpath:jdbc-h2.properties"/>
    </beans>

    <beans profile="mysql">
        <context:property-placeholder location="classpath:jdbc-mysql.properties"/>
    </beans>

    <!--Database Config-->
    <beans profile="dbPrepare">
        <jdbc:initialize-database data-source="dataSource">
            <jdbc:script encoding="utf-8" location="classpath:/sql/db_showcase_schema.sql"/>
            <jdbc:script encoding="utf-8" location="classpath:/sql/db_showcase_data.sql"/>
        </jdbc:initialize-database>
    </beans>

</beans>
